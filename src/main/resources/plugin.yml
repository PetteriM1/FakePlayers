name: Fake
version: "${pom.version}"
api: ["1.0.7"]
author: PetteriM1
main: idk.plugin.fake.Main

commands:
  fake:
    description: Add or remove fake player
    usage: "/fake <add|remove> <name>"
    permission: fake.use
permissions:
  fake.use:
    description: "Allow usage of /fake"
    default: op